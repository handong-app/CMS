export interface ProgramItemProps {
  programId: string;
  clubId: string;
  clubSlug: string;
  name: string;
  description: string;
  startDate: string;
  endDate: string;
  courses: string[];
  slug: string;
}
